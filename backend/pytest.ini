[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: Unit tests (fast, isolated)
    integration: Integration tests (full request cycle)
    slow: Tests that may take > 2 seconds

addopts =
    -ra
    --strict-markers
    --tb=short
